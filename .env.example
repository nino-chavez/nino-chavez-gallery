# SmugMug API Credentials
# Get these from: https://api.smugmug.com/api/developer/apply
SMUGMUG_API_KEY=
SMUGMUG_API_SECRET=
SMUGMUG_ACCESS_TOKEN=
SMUGMUG_ACCESS_TOKEN_SECRET=
SMUGMUG_USERNAME=

# Vision API Provider
# Options: 'auto' (default), 'claude', 'openai', 'gemini'
VISION_PROVIDER=auto

# Anthropic Claude API (RECOMMENDED - 64% cheaper than OpenAI)
# Get from: https://console.anthropic.com/
# Cost: ~$3.60 per 1,000 photos
ANTHROPIC_API_KEY=

# OpenAI API (Fallback)
# Get from: https://platform.openai.com/api-keys
# Cost: ~$10 per 1,000 photos
OPENAI_API_KEY=

# Google Gemini API (Optional, but very cost-effective)
# Get from: https://ai.google.dev/
# Cost: ~$0.17 per 1,000 photos (Flash) or ~$28 per 1,000 photos (Pro)
GOOGLE_API_KEY=

# Gemini Model Selection (IMPORTANT for cost control!)
# Flash models: models/gemini-1.5-flash-002 (recommended, 17x cheaper)
# Pro models: models/gemini-1.5-pro-002, models/gemini-2.5-pro-002 (best quality, expensive)
# WARNING: Do NOT use 'latest' - it may resolve to expensive Pro models!
GEMINI_MODEL=models/gemini-1.5-flash-002

# SmugMug Image Size Optimization (CRITICAL for cost savings!)
# Options: O=Original, D=Display, X3=X3Large, L=Large, M=Medium, S=Small
# Medium (M) STRONGLY recommended - saves 85-88% on costs with no quality loss for metadata
# Your images: Display=17MB (~50K tokens), Medium=87KB (~300 tokens) - 166x difference!
# Use larger sizes ONLY if you need to read small text or analyze pixel-level details
SMUGMUG_IMAGE_SIZE=M

# Pinecone Vector Database
# Get from: https://www.pinecone.io/
PINECONE_API_KEY=
PINECONE_INDEX_NAME=gallery-photos

# Upstash Redis (Caching)
# Get from: https://console.upstash.com/
UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=

# Vercel Analytics (Optional)
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# Sentry Error Tracking (Optional)
SENTRY_DSN=

# Public URLs
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_GALLERY_URL=http://localhost:3000
